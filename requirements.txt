# ── Core AI / LLM ─────────────────────────────────────────────────────────────
openai>=1.0.0               # DeepSeek V3 + OpenAI-compatible APIs
anthropic>=0.20.0           # Claude (optional, for claude_agent)

# ── Voice pipeline ────────────────────────────────────────────────────────────
pipecat-ai[deepgram,cartesia,silero]>=0.0.47  # Real-time voice pipeline
deepgram-sdk>=3.0.0         # STT (nova-3 model)
cartesia                    # TTS

# ── Memory / vector store ─────────────────────────────────────────────────────
chromadb>=0.4.0             # L1 semantic memory (ChromaDB)

# ── Google APIs ───────────────────────────────────────────────────────────────
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0

# ── AWS ───────────────────────────────────────────────────────────────────────
boto3>=1.26.0               # Lightsail / CloudWatch metrics

# ── Web / HTTP ────────────────────────────────────────────────────────────────
requests>=2.28.0
websockets>=12.0
tavily-python>=0.3.0        # Research / web search

# ── Documents ─────────────────────────────────────────────────────────────────
python-docx>=1.0.0          # DOCX report generation
weasyprint>=60.0            # PDF export (optional)
Pillow>=10.0.0              # Image handling

# ── Automation / Vision ───────────────────────────────────────────────────────
PyAutoGUI>=0.9.54           # Mouse control, screen interaction
openwakeword>=0.5.0         # Local wake word detection
sounddevice>=0.4.6          # Microphone audio capture (voice auth gate)

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # .env loading
numpy>=1.24.0               # Numeric ops (wake word, audio)
loguru>=0.7.0               # Structured logging
