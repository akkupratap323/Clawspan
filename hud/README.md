# HUD Video Setup

Place your video file in this directory and name it `video.mp4`.

The HUD will automatically load and loop it in the right-side video panel.

Supported formats: `.mp4`, `.webm`, `.mov`

If you use a different filename, update the `<source src="...">` line in `index.html`.
